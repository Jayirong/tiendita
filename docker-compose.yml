version: '1'
services:
  microservicio:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    environment:
      - spring.datasource.url=jdbc:oracle:thin@fstack_tp?TNS _ADMIN=/app/wallet_fstack/
      - SPRING.DATASOURCE.USERNAME=TienditaUser
      - SPRING.DATASOURCE.PASSWORD=OracleCloud123
    restart: always